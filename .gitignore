# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
/build
next-env.d.ts

# production
/dist

# testing
/coverage
/test-results
/playwright-report

# database
*.db
*.db-journal
/prisma/dev.db*

# secrets — never commit
.env
.env.*
!.env.example

# guest-uploaded payment proofs (contain personal financial detail)
/public/uploads
/uploads

# os / editor
.DS_Store
Thumbs.db
*.pem
.vscode/*
!.vscode/extensions.json
.idea

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# vercel
.vercel

# NOTE: /public/media IS committed on purpose — Vercel serves it as static
# assets and it has to be in the repo. Source originals stay outside the
# project in "Racho Felipe/" and are never copied in.

# local dev database (embedded postgres data dir)
/.pgdata
/.pglite

# logs from scripts/share.ps1
/.logs

# The owner's guide. Kept out of the repo on purpose: the repo is public, and
# this booklet names the admin sign-in username and walks through the admin
# screens. The username was deliberately scrubbed from the tracked files in
# bb6418e; committing this would put it straight back.
/docs/
